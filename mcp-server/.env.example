PORT=3847

# Path to site credentials config (optional — see resolution order below).
# If unset, the server looks for:
#   1. .plinth.json in the current working directory (per-repo config)
#   2. sites.json in this mcp-server directory (central multi-site config)
#
# PLINTH_CONFIG=/path/to/.plinth.json
